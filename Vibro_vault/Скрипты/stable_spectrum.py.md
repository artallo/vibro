---
tags: [vibro, скрипт, основной]
файл: stable_spectrum.py
---

# stable_spectrum.py — устойчивый спектр

## Для чего

Отвечает на вопрос, у которого есть повторяемый ответ: какие пики записи
больше погрешности той спектральной оценки, которая их дала. Если ничего
не выделяется, отчёт так и пишет и называет предел в дБ, который был
нужен. Это тоже устойчивый результат. Метод описан в
[[Устойчивый спектр — метод]].

## Ключи

| Ключ             | По умолчанию                                                                                | Что делает                                                                 |
| ---------------- | ------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| `пути к .npz`    | обязательно                                                                                 | одна или несколько записей                                                 |
| `--output папка` | `stable_results/<запись>/` для одной записи, `stable_results/multi_<время>/` для нескольких | куда писать                                                                |
| `--band MIN MAX` | из конфига, 0.5 15                                                                          | полоса анализа в Гц; для техники выше 15 Гц берите `0.5 100`               |
| `--nperseg N …`  | `1024 2048 4096`                                                                            | длины отрезков; каждая пишется в свою папку, см. [[Разрешение nperseg]]    |
| `--probe F …`    | нет                                                                                         | что стоит на заданных частотах, по всем осям, с верхней границей 95 %      |
| `--alpha`        | 0.01                                                                                        | допустимая вероятность хотя бы одного ложного пика на всю полосу и все оси |

## Примеры

Одна запись, три разрешения:

```bash
python stable_spectrum.py results/20260916_193930_ODR250_run01_raw.npz
```

Три записи в одну папку, полоса до 100 Гц:

```bash
python stable_spectrum.py tumen_results/20260916_*_raw.npz --band 0.5 100 --output stable_results/260916_wide
```

Проверка заданных частот:

```bash
python stable_spectrum.py results/<запись>_raw.npz --probe 0.62 1.60 11.35
```

Только одно разрешение:

```bash
python stable_spectrum.py results/<запись>_raw.npz --nperseg 4096
```

## Что пишет

```
stable_results/<запись>/
├── comparison.txt                 сводка трёх разрешений
├── figure_compare_<запись>.png    спектры трёх разрешений наложены
├── nperseg_1024/
│   ├── stable_report.txt          отчёт
│   ├── stable_frequencies.csv     таблица значимых пиков
│   ├── figure_dominant_<запись>.png        картинка для показа
│   └── figure_stable_spectrum_<запись>.png превышение в дБ против полосы шума
├── nperseg_2048/ …
└── nperseg_4096/ …
```

## Как читать отчёт

| Строка | Смысл |
|---|---|
| `Resolution` | nperseg, ширина бина, сколько отрезков усреднено |
| `Significance threshold: z >= …` | порог значимости |
| `Recording check` | уровень по осям, выбросы в пакетах, скачки на стыках пакетов, разброс частоты опроса |
| `Detection limit` | какое превышение в дБ было бы нужно, чтобы пик признали |
| `Strongest candidate` | самый высокий кандидат, если ничего не прошло порог |
| Таблица пиков | частота, превышение, погрешность, z, z на половинах, `Persistent` |
| `Support` на картинке | в скольких из 16 независимых окон частота видна; случайно набирается около 0,8 |
| `Requested frequencies` | ответ на `--probe`: «present» или «absent above X dB» |

`Persistent = yes` значит, что пик виден в обеих половинах записи, которые
не делят ни одного отсчёта.

> [!warning] Порог для `--probe`
> Сейчас проверка заданных частот применяет тот же порог, что и поиск по
> всей полосе, около 3,9 при 256 пакетах. Для частот, выбранных заранее,
> честный порог ниже, около 3,1 для девяти проверок. Это задача на
> доработку, см. [[Открытые задачи]].

## Проверка записи

- **Выброс в пакете**: пик больше 12 робастных сигм. В пакете 1 это
  стартовый переход датчика, в остальных — удар.
- **Громкие пакеты**: мощность в полосе больше медианы в 8 раз.
- **Тихая ось**: уровень меньше 5 % от самой громкой, проверьте провод.
- **Разброс частоты опроса** больше 1000 ppm: частоты размываются.
- **Скачок на стыке пакетов**: сбой или потеря отсчётов. Тихую потерю
  пакета в шумовой записи так не увидеть, отчёт об этом пишет.
